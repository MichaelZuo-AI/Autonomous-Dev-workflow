# Project: [AppName]

## Architecture Decision Records

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Framework | [e.g., Next.js / Express / Expo] | [why] |
| Language | [e.g., TypeScript] | [why] |
| State management | [e.g., Zustand / Redux / Context] | [why] |
| API layer | [e.g., React Query + Axios / tRPC] | [why] |
| Auth | [e.g., NextAuth / JWT / OAuth] | [why] |
| Testing | [e.g., Vitest / Jest + Testing Library] | [why] |
| Database | [e.g., PostgreSQL / SQLite / none] | [why] |

## Code Navigation Map

```
src/
├── features/        # Feature-scoped modules
├── components/      # Shared UI components
├── domain/          # Core business logic
├── services/        # API clients / external integrations
└── constants/       # No magic strings
```

## Current Sprint Context

- **Active feature:** [feature name]
- **Completed features:** [list]
- **Known issues / blockers:** [list]

## Conventions (MUST follow)

- [Component / module pattern]: see `src/[path]` as reference
- Error handling: [chosen pattern, e.g., Result<T, E>, try/catch, error boundaries]
- Every module requires: [test expectations, e.g., unit test + integration test]
- No magic strings — use constants from `src/constants/`

## Gotchas & Lessons Learned

<!-- Add entries as issues are discovered and resolved -->
<!-- Format: - [YYYY-MM-DD] Description of issue + fix -->
