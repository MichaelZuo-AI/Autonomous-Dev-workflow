# Project: [AppName]

## Architecture Decision Records

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Framework | [e.g., React Native / Expo] | [why] |
| State management | [e.g., Zustand] | [why] |
| Navigation | [e.g., Expo Router] | [why] |
| API layer | [e.g., React Query + Axios] | [why] |
| Auth | [e.g., Expo SecureStore + JWT] | [why] |
| Testing | [e.g., Jest + React Native Testing Library] | [why] |

## Code Navigation Map

```
src/
├── features/        # Feature-scoped modules
├── components/ui/   # Design system components (Storybook-driven)
├── domain/          # Core business logic
├── services/        # API clients
└── constants/       # No magic strings
```

## Current Sprint Context

- **Active feature:** [feature name]
- **Completed features:** [list]
- **Known issues / blockers:** [list]

## Conventions (MUST follow)

- Component pattern: see `src/components/ui/Button.tsx` as reference
- Error handling: use `Result<T, E>` pattern (never throw)
- Every component requires: snapshot test + behavior test
- No magic strings — use constants from `src/constants/`
- All styles must use design tokens from `DESIGN_TOKENS.md`

## Gotchas & Lessons Learned

<!-- Add entries as issues are discovered and resolved -->
<!-- Format: - [YYYY-MM-DD] Description of issue + fix -->
